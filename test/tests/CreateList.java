package tests;

import com.codename1.testing.AbstractTest;

import com.codename1.ui.Display;

public class CreateList extends AbstractTest {
    public boolean runTest() throws Exception {
        waitForFormTitle("TodoCatalyst");
        clickButtonByLabel("Lists");
        waitForFormTitle("Lists");
        Display.getInstance().getCurrent().setName("Form_1");
        assertEqual(getToolbarCommands().length, 9);
        executeToolbarCommandAtOffset(4);
        waitForFormName("Form_1");
        waitForFormName("Form_1");
        waitForFormTitle("List");
        Display.getInstance().getCurrent().setName("Form_2");
        setText(new int[]{0}, "List1");
        assertEqual(getToolbarCommands().length, 6);
        executeToolbarCommandAtOffset(0);
        waitForFormName("Form_1");
        assertLabel(null);
        assertLabel("");
        assertLabel("0:44/0:26");
        assertLabel("15/22");
        assertLabel("");
        assertLabel(null);
        assertLabel("");
        assertLabel("");
        assertLabel("0");
        assertLabel("");
        assertLabel(null);
        assertLabel("");
        assertLabel("");
        assertLabel("0");
        assertLabel("");
        assertLabel(null);
        assertLabel("");
        assertLabel("0:04");
        assertLabel("1/1");
        assertLabel("");
        assertLabel(null);
        assertLabel("");
        assertLabel("");
        assertLabel("0");
        assertLabel("");
        assertLabel(null);
        assertLabel("");
        assertLabel("");
        assertLabel("0");
        assertLabel("");
        assertLabel(null);
        assertLabel("");
        assertLabel("[1:44]");
        assertLabel("7/30");
        assertLabel("");
        assertLabel(null);
        assertLabel("");
        assertLabel("1:08/0:15");
        assertLabel("9/9");
        assertLabel("");
        assertLabel(null);
        assertLabel("");
        assertLabel("0:08");
        assertLabel("2/2");
        assertLabel("");
        assertLabel(null);
        assertLabel("");
        assertLabel("0:12/0:10");
        assertLabel("37/45");
        assertLabel("");
        assertLabel(null);
        assertLabel("");
        assertLabel("0:36/0:12");
        assertLabel("12/12");
        assertLabel("");
        assertLabel(null);
        assertLabel("");
        assertLabel("0:04/0:04");
        assertLabel("12/12");
        assertLabel("");
        assertLabel(null);
        assertLabel("");
        assertLabel("0:24/1:04");
        assertLabel("5/5");
        assertLabel("");
        assertLabel(null);
        assertLabel("");
        assertLabel("0:04/0:04");
        assertLabel("8/8");
        assertLabel("");
        assertLabel(null);
        assertLabel("");
        assertLabel("0:10/0:10");
        assertLabel("3/3");
        assertLabel("");
        assertLabel(null);
        assertLabel("");
        assertLabel("0:15/0:04");
        assertLabel("2/5");
        assertLabel("");
        assertLabel(null);
        assertLabel("");
        assertLabel("0:06/0:08");
        assertLabel("2/4");
        assertLabel("");
        assertLabel(null);
        assertLabel("");
        assertLabel("2:01/2:13");
        assertLabel("63/74");
        assertLabel("");
        assertLabel(null);
        assertLabel("");
        assertLabel("0:50/0:29");
        assertLabel("57/64");
        assertLabel("");
        assertLabel(null);
        assertLabel("");
        assertLabel("");
        assertLabel("0");
        assertLabel("");
        assertLabel(null);
        assertLabel("");
        assertLabel("");
        assertLabel("0/1");
        assertLabel("");
        assertLabel(null);
        assertLabel("");
        assertLabel("");
        assertLabel("1/4");
        assertLabel("");
        assertLabel(null);
        assertLabel("");
        assertLabel("");
        assertLabel("0/2");
        assertLabel("");
        assertLabel(null);
        assertLabel("");
        assertLabel("2:05/2:50");
        assertLabel("68/78");
        assertLabel("");
        assertLabel(null);
        assertLabel("");
        assertLabel("");
        assertLabel("0");
        assertLabel("");
        assertTextArea("RepeatTestList");
        assertTextArea("lll");
        assertTextArea("nnew");
        assertTextArea("TestAlarms");
        assertTextArea("newList");
        assertTextArea("insertList");
        assertTextArea("testlist2[W]");
        assertTextArea("lxx");
        assertTextArea("L-rep");
        assertTextArea("L10");
        assertTextArea("L-A");
        assertTextArea("L3");
        assertTextArea("L4");
        assertTextArea("L1");
        assertTextArea("ListFromTest");
        assertTextArea("testlist");
        assertTextArea("L2");
        assertTextArea("Inbox");
        assertTextArea("Touched");
        assertTextArea("Today");
        assertTextArea("Overdue");
        assertTextArea("Diary");
        assertTextArea("Log");
        assertTextArea("");
        assertTextArea("List1");
        return true;
    }
}
